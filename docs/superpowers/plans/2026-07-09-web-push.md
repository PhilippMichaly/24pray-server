# Web-Push / PWA (Backlog 7) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Eingeloggte Nutzer können pro Gerät Web-Push aktivieren (Opt-in auf der Profil-Seite) und bekommen Stunden-Erinnerungen sowie Owner-Updates zusätzlich als Push — self-hosted VAPID, kein Drittdienst.

**Architecture:** API: neues Modell `PushSubscription` (endpoint unique, an User gebunden — Gäste bleiben Mail-only), Routen `GET /push/vapid-key` + `POST/DELETE /me/push-subscriptions`, Sende-Lib `src/lib/push.ts` (`createPushSender` via `web-push`, fail-closed ohne VAPID-Env; `pushToUsers` mit automatischem Aufräumen abgelaufener Subscriptions bei 404/410). Zwei Hooks: Reminder-Job (`jobs.ts`, Zweitkanal neben Mail, nur Slots mit `userId`) und Update-Fanout (`community.ts`, Empfänger-E-Mail → User → Subscriptions). Web: statischer Service Worker `public/sw.js` (Notification anzeigen, Klick öffnet URL), Client-Lib `src/lib/push.ts` (subscribe/unsubscribe), Opt-in-Karte auf der Profil-Seite. Der Sender ist als `pushSender`-Dep injizierbar — Tests brauchen keine echten VAPID-Keys.

**Tech Stack:** NEU: `web-push` + `@types/web-push` (api) — einzige neue Dependency des Batches, für VAPID-Signierung unvermeidlich. Sonst Bestand.

## Global Constraints

- Repos: API = `/home/pmi/24pray-api`, Web = `/home/pmi/24pray-web` (Branch `main`). Committen ja, **NICHT pushen** (git).
- Fail-closed-Muster wie FUNNEL/FEEDBACK: Ohne `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY` antworten alle Push-Routen 404, Sender ist `null`, Hooks no-op. Keys werden NIE committed; Erzeugung beim Deploy: `npx web-push generate-vapid-keys`.
- Push-Payload enthält NUR: title, body (Wachen-Titel/Update-Anriss), url. Keine E-Mails/IDs von Dritten.
- Push ist ZUSATZ-Kanal: Mail-Versand bleibt in beiden Hooks unverändert; Push-Fehler dürfen nie Mail/Request kippen (fire-and-forget, `.catch`).
- **NIEMALS `next build` lokal**; keinen Dev-Server killen; **NIEMALS `prisma migrate dev`** — manuelle Migrations-Prozedur (Ordner von Hand, python3-sqlite `timeout=30`, `migrate resolve --applied`; bei „database is locked" deterministisch: manueller `_prisma_migrations`-INSERT mit sha256-Checksum = etabliertes Muster; dann `npx prisma generate && touch src/server.ts`).
- i18n Web: neue Keys in ALLE 5 Kataloge (he/ar verbatim aus diesem Plan).
- Suiten: api vitest+tsc; web vitest+tsc+lint. TDD, Testdaten-Prefix `un7-`.
- Commit-Trailer:
  `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` und
  `Claude-Session: https://claude.ai/code/session_011CPPASHZYersFD37Auq5kB`

---

### Task 1: API — Dependency, Modell, Sende-Lib, Subscription-Routen

**Files:**
- Modify: `/home/pmi/24pray-api/package.json` (via `npm install web-push && npm install -D @types/web-push`)
- Modify: `/home/pmi/24pray-api/prisma/schema.prisma` (Modell + User-Relation)
- Create: `/home/pmi/24pray-api/prisma/migrations/20260709200000_add_push_subscriptions/migration.sql`
- Modify: `/home/pmi/24pray-api/src/env.ts` (3 VAPID-Vars)
- Create: `/home/pmi/24pray-api/src/lib/push.ts`
- Create: `/home/pmi/24pray-api/src/routes/push.ts`
- Modify: `/home/pmi/24pray-api/src/app.ts` (BuildAppDeps.pushSender + Registrierung)
- Test: `/home/pmi/24pray-api/src/lib/push.test.ts`, `/home/pmi/24pray-api/src/routes/push.test.ts`
- Modify: `/home/pmi/24pray-api/deploy/README.md` (Env-Block: VAPID-Zeilen + generate-Kommando)

**Interfaces (Tasks 2+3 verlassen sich exakt darauf):**

```ts
// src/lib/push.ts
export interface PushPayload { title: string; body: string; url: string }
export interface PushSubscriptionRecord { endpoint: string; p256dh: string; auth: string }
export type PushSender = (sub: PushSubscriptionRecord, payload: PushPayload) => Promise<void>;
export function createPushSender(env: Env): PushSender | null; // null ohne VAPID-Keys
export async function pushToUsers(prisma: PrismaClient, sender: PushSender, userIds: string[], payload: PushPayload): Promise<void>;
```

- `POST /me/push-subscriptions` Body `{ endpoint: string.url, keys: { p256dh: string, auth: string } }` → 204 (Upsert per endpoint, userId = eingeloggter User); 401 ohne Login; 404 ohne VAPID-Env.
- `DELETE /me/push-subscriptions` Body `{ endpoint }` → 204 (löscht nur eigene); 401/404 analog.
- `GET /push/vapid-key` → `{ key: string }` (public); 404 ohne VAPID-Env.
- `env.VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT` — alle `z.string().default('')`, Subject-Beispiel `mailto:no-reply@24pray.org`.

- [ ] **Step 1: Dependencies installieren**

```bash
cd /home/pmi/24pray-api && npm install web-push && npm install -D @types/web-push
```

- [ ] **Step 2: Failing Tests schreiben**

`/home/pmi/24pray-api/src/lib/push.test.ts`:

```ts
import { describe, it, expect, vi } from 'vitest';
import { makeTestDb, type TestDb } from '../test/helpers.js';
import { pushToUsers, createPushSender } from './push.js';
import { parseEnv } from '../env.js';

describe('push lib (Backlog 7)', () => {
  it('createPushSender ist null ohne VAPID-Keys', () => {
    expect(createPushSender(parseEnv({ APP_URL: 'http://localhost:3000' }))).toBeNull();
  });

  it('pushToUsers sendet an alle Subs der User und räumt 410er auf', async () => {
    const db: TestDb = await makeTestDb();
    try {
      const u = await db.prisma.user.create({ data: { email: 'un7-push@example.com', name: 'un7' } });
      await db.prisma.pushSubscription.createMany({
        data: [
          { endpoint: 'https://push.example/ok', p256dh: 'k1', auth: 'a1', userId: u.id },
          { endpoint: 'https://push.example/gone', p256dh: 'k2', auth: 'a2', userId: u.id },
        ],
      });
      const sent: string[] = [];
      const sender = vi.fn(async (sub: { endpoint: string }) => {
        sent.push(sub.endpoint);
        if (sub.endpoint.endsWith('/gone')) {
          const e = new Error('gone') as Error & { statusCode?: number };
          e.statusCode = 410;
          throw e;
        }
      });
      await pushToUsers(db.prisma, sender, [u.id], { title: 't', body: 'b', url: 'https://x/p' });
      expect(sent.sort()).toEqual(['https://push.example/gone', 'https://push.example/ok']);
      const left = await db.prisma.pushSubscription.findMany({ where: { userId: u.id } });
      expect(left.map((s) => s.endpoint)).toEqual(['https://push.example/ok']); // 410er gelöscht
    } finally { await db.cleanup(); }
  });
});
```

`/home/pmi/24pray-api/src/routes/push.test.ts` (Kopf nach dem Muster von `feedback.test.ts`; buildApp-Env MIT `VAPID_PUBLIC_KEY: 'un7-pub', VAPID_PRIVATE_KEY: 'un7-priv', VAPID_SUBJECT: 'mailto:un7@example.com'`):

```ts
describe('Push-Subscription-Routen (Backlog 7)', () => {
  it('vapid-key liefert den Public Key', async () => {
    const res = await app.inject({ method: 'GET', url: '/push/vapid-key' });
    expect(res.statusCode).toBe(200);
    expect(res.json().key).toBe('un7-pub');
  });

  it('Subscribe braucht Login; Upsert per endpoint; Delete löscht nur eigene', async () => {
    const sub = { endpoint: 'https://push.example/un7-a', keys: { p256dh: 'p', auth: 'a' } };
    expect((await app.inject({ method: 'POST', url: '/me/push-subscriptions', payload: sub })).statusCode).toBe(401);
    const cookieA = await loginAs('un7-a@example.com');
    expect((await app.inject({ method: 'POST', url: '/me/push-subscriptions', cookies: { session: cookieA }, payload: sub })).statusCode).toBe(204);
    // Upsert: gleicher endpoint nochmal → immer noch genau 1 Row
    expect((await app.inject({ method: 'POST', url: '/me/push-subscriptions', cookies: { session: cookieA }, payload: sub })).statusCode).toBe(204);
    expect(await db.prisma.pushSubscription.count({ where: { endpoint: sub.endpoint } })).toBe(1);
    // Fremder darf nicht löschen
    const cookieB = await loginAs('un7-b@example.com');
    await app.inject({ method: 'DELETE', url: '/me/push-subscriptions', cookies: { session: cookieB }, payload: { endpoint: sub.endpoint } });
    expect(await db.prisma.pushSubscription.count({ where: { endpoint: sub.endpoint } })).toBe(1);
    // Eigentümer löscht
    await app.inject({ method: 'DELETE', url: '/me/push-subscriptions', cookies: { session: cookieA }, payload: { endpoint: sub.endpoint } });
    expect(await db.prisma.pushSubscription.count({ where: { endpoint: sub.endpoint } })).toBe(0);
  });

  it('ohne VAPID-Env: alle Push-Routen 404 (fail-closed)', async () => {
    const { buildApp: build } = await import('../app.js');
    const bare = await build({
      prisma: db.prisma,
      env: parseEnv({ APP_URL: 'http://localhost:3000' }),
      mailer: { async sendMagicLink() {} },
    });
    await bare.ready();
    try {
      expect((await bare.inject({ method: 'GET', url: '/push/vapid-key' })).statusCode).toBe(404);
      expect((await bare.inject({ method: 'POST', url: '/me/push-subscriptions', payload: { endpoint: 'https://x/y', keys: { p256dh: 'p', auth: 'a' } } })).statusCode).toBe(404);
    } finally { await bare.close(); }
  });
});
```

- [ ] **Step 3: Tests laufen lassen — müssen scheitern**

Run: `cd /home/pmi/24pray-api && npx vitest run src/lib/push.test.ts src/routes/push.test.ts`
Expected: FAIL — Module/Model fehlen.

- [ ] **Step 4: Schema + Migration**

`prisma/schema.prisma` — `model User`: Relation ergänzen `pushSubs PushSubscription[]`; neues Modell (nach `FunnelCount`):

```prisma
// Web-Push (Backlog 7): eine Subscription pro Browser/Gerät, an den eingeloggten User gebunden
// (Gäste bleiben Mail-only). endpoint ist der eindeutige Browser-Push-Endpoint; 404/410 beim
// Senden = abgelaufen → Row wird automatisch aufgeräumt (lib/push.ts).
model PushSubscription {
  id        String   @id @default(cuid())
  endpoint  String   @unique
  p256dh    String
  auth      String
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())

  @@index([userId])
}
```

`prisma/migrations/20260709200000_add_push_subscriptions/migration.sql`:

```sql
CREATE TABLE "PushSubscription" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "endpoint" TEXT NOT NULL,
    "p256dh" TEXT NOT NULL,
    "auth" TEXT NOT NULL,
    "userId" TEXT NOT NULL,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT "PushSubscription_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User" ("id") ON DELETE CASCADE ON UPDATE CASCADE
);
CREATE UNIQUE INDEX "PushSubscription_endpoint_key" ON "PushSubscription"("endpoint");
CREATE INDEX "PushSubscription_userId_idx" ON "PushSubscription"("userId");
```

Anwenden per manueller Prozedur, dann `npx prisma generate && touch src/server.ts`.

- [ ] **Step 5: env + Lib + Routen**

`src/env.ts` — nach `FEEDBACK_TO`:

```ts
  // Web-Push VAPID (Backlog 7). Alle drei leer = Push-Feature aus (Routen 404, Hooks no-op).
  // Erzeugen: npx web-push generate-vapid-keys
  VAPID_PUBLIC_KEY: z.string().default(''),
  VAPID_PRIVATE_KEY: z.string().default(''),
  VAPID_SUBJECT: z.string().default(''), // z. B. mailto:no-reply@24pray.org
```

`src/lib/push.ts`:

```ts
import webpush from 'web-push';
import type { PrismaClient } from '@prisma/client';
import type { Env } from '../env.js';

export interface PushPayload { title: string; body: string; url: string }
export interface PushSubscriptionRecord { endpoint: string; p256dh: string; auth: string }
export type PushSender = (sub: PushSubscriptionRecord, payload: PushPayload) => Promise<void>;

/** Echter web-push-Sender — null, wenn VAPID nicht konfiguriert (fail-closed). */
export function createPushSender(env: Env): PushSender | null {
  if (!env.VAPID_PUBLIC_KEY || !env.VAPID_PRIVATE_KEY) return null;
  const subject = env.VAPID_SUBJECT || 'mailto:no-reply@24pray.org';
  return async (sub, payload) => {
    await webpush.sendNotification(
      { endpoint: sub.endpoint, keys: { p256dh: sub.p256dh, auth: sub.auth } },
      JSON.stringify(payload),
      { vapidDetails: { subject, publicKey: env.VAPID_PUBLIC_KEY, privateKey: env.VAPID_PRIVATE_KEY } },
    );
  };
}

/** An alle Geräte der genannten User senden; abgelaufene Subscriptions (404/410) aufräumen.
 *  Fehler einzelner Endpunkte stoppen nie die übrigen. */
export async function pushToUsers(
  prisma: PrismaClient,
  sender: PushSender,
  userIds: string[],
  payload: PushPayload,
): Promise<void> {
  if (userIds.length === 0) return;
  const subs = await prisma.pushSubscription.findMany({ where: { userId: { in: userIds } } });
  await Promise.all(subs.map(async (s) => {
    try {
      await sender({ endpoint: s.endpoint, p256dh: s.p256dh, auth: s.auth }, payload);
    } catch (err) {
      const status = (err as { statusCode?: number }).statusCode;
      if (status === 404 || status === 410) {
        await prisma.pushSubscription.delete({ where: { id: s.id } }).catch(() => {});
      } else {
        console.error(`[push] send failed for ${s.endpoint}:`, err);
      }
    }
  }));
}
```

`src/routes/push.ts`:

```ts
import type { FastifyInstance } from 'fastify';
import type { PrismaClient } from '@prisma/client';
import { z } from 'zod';
import { requireUser } from '../plugins/auth.js';
import type { Env } from '../env.js';

const SubscribeBody = z.object({
  endpoint: z.string().url().max(1000),
  keys: z.object({ p256dh: z.string().min(1).max(300), auth: z.string().min(1).max(100) }),
});
const UnsubscribeBody = z.object({ endpoint: z.string().url().max(1000) });

export function pushRoutes(app: FastifyInstance, deps: { prisma: PrismaClient; env?: Env }) {
  const { prisma, env } = deps;
  const enabled = () => Boolean(env?.VAPID_PUBLIC_KEY && env?.VAPID_PRIVATE_KEY);

  app.get('/push/vapid-key', async (req, reply) => {
    if (!enabled()) return reply.code(404).send({ message: 'Nicht gefunden' });
    return { key: env!.VAPID_PUBLIC_KEY };
  });

  app.post('/me/push-subscriptions', {
    config: { rateLimit: { max: 10, timeWindow: '1 minute' } },
  }, async (req, reply) => {
    if (!enabled()) return reply.code(404).send({ message: 'Nicht gefunden' });
    const user = requireUser(req);
    const body = SubscribeBody.parse(req.body);
    // Upsert per endpoint: Gerät kann den Besitzer wechseln (neuer Login im selben Browser)
    await prisma.pushSubscription.upsert({
      where: { endpoint: body.endpoint },
      update: { p256dh: body.keys.p256dh, auth: body.keys.auth, userId: user.id },
      create: { endpoint: body.endpoint, p256dh: body.keys.p256dh, auth: body.keys.auth, userId: user.id },
    });
    return reply.code(204).send();
  });

  app.delete('/me/push-subscriptions', async (req, reply) => {
    if (!enabled()) return reply.code(404).send({ message: 'Nicht gefunden' });
    const user = requireUser(req);
    const { endpoint } = UnsubscribeBody.parse(req.body);
    await prisma.pushSubscription.deleteMany({ where: { endpoint, userId: user.id } });
    return reply.code(204).send();
  });
}
```

`src/app.ts` — `BuildAppDeps` um `pushSender?: PushSender | null` erweitern; Default beim Destructuring: `const pushSender = deps.pushSender !== undefined ? deps.pushSender : createPushSender(env);` (Imports aus `./lib/push.js`); Registrierung `pushRoutes(app, { prisma, env });` — UND `pushSender` an `communityRoutes` durchreichen (Task 2 nutzt ihn dort; Signatur-Erweiterung dort). Für den Reminder-Job: prüfe, wo `sendDueReminders` aufgerufen wird (`src/server.ts` — Interval) und reiche `pushSender` dort mit durch (Task 2 ändert die Signatur; in DIESEM Task nur `app.ts`/Dep-Grundlage).

`deploy/README.md` — Env-Block nach `FEEDBACK_TO=`:

```
# Web-Push VAPID (Backlog 7) — erzeugen mit: npx web-push generate-vapid-keys; leer = Push aus
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:no-reply@24pray.org
```

- [ ] **Step 6: Tests laufen lassen — grün (komplette Suite)**

Run: `cd /home/pmi/24pray-api && npx vitest run && npx tsc --noEmit`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
cd /home/pmi/24pray-api
git add package.json package-lock.json prisma/schema.prisma prisma/migrations/20260709200000_add_push_subscriptions/ src/env.ts src/lib/push.ts src/lib/push.test.ts src/routes/push.ts src/routes/push.test.ts src/app.ts deploy/README.md
git commit -m "feat(push): VAPID-Grundlage — PushSubscription-Modell, Sende-Lib, Subscription-Routen (Backlog 7)"
```

---

### Task 2: API — Push-Hooks: Stunden-Erinnerung + Owner-Updates

**Files:**
- Modify: `/home/pmi/24pray-api/src/lib/jobs.ts` (`sendDueReminders`)
- Modify: `/home/pmi/24pray-api/src/server.ts` (Job-Aufruf: pushSender durchreichen — Aufrufstelle vorher per grep lokalisieren)
- Modify: `/home/pmi/24pray-api/src/routes/community.ts` (Update-Fanout)
- Modify: `/home/pmi/24pray-api/src/app.ts` (pushSender an communityRoutes)
- Test: `/home/pmi/24pray-api/src/lib/jobs.test.ts`, `/home/pmi/24pray-api/src/routes/community.test.ts` (erweitern)

**Interfaces:**
- Consumes: `PushSender`/`pushToUsers`/`PushPayload` aus Task 1.
- Produces: `sendDueReminders(...)` akzeptiert zusätzlich optionalen `pushSender: PushSender | null`; `communityRoutes`-Deps zusätzlich `pushSender?: PushSender | null`.

- [ ] **Step 1: Failing Tests schreiben**

In `jobs.test.ts` (Fixtures der Datei nutzen; Fake-Sender = Capture-Array):

```ts
it('Backlog 7: fällige Erinnerung pusht zusätzlich an die Geräte des Users', async () => {
  // Muster der bestehenden Reminder-Tests der Datei kopieren (User + Projekt + fälliger Slot
  // mit userId), zusätzlich: pushSubscription für den User anlegen.
  const pushed: { endpoint: string; payload: { title: string; url: string } }[] = [];
  const fakeSender = async (sub: { endpoint: string }, payload: never) => { pushed.push({ endpoint: sub.endpoint, payload }); };
  // sendDueReminders mit fakeSender als zusätzlichem Argument aufrufen (Signatur siehe Implementierung)
  // Assertions:
  // - pushed enthält 1 Eintrag mit dem Sub-Endpoint des Users
  // - payload.title enthält den Projekt-Titel, payload.url enthält /projects/<id>
  // - Mail-Versand (bestehendes reminders-Capture) ist UNVERÄNDERT auch erfolgt (Zweitkanal!)
});
it('Backlog 7: Gast-Slot (ohne userId) erzeugt keinen Push, Job läuft fehlerfrei', async () => { /* analog, guestEmail-Slot, pushed bleibt leer */ });
```

In `community.test.ts` (bestehendes Update-Fanout-describe erweitern):

```ts
it('Backlog 7: Owner-Update pusht an Teilnehmer mit Konto+Subscription (Gäste nicht)', async () => {
  // Setup wie setupProjectWithParticipants; zusätzlich pushSubscription für den member-User.
  // buildApp bekommt pushSender: fakeSender (Capture).
  // Owner postet Update → vi.waitFor: genau 1 Push (member), Gast (nur E-Mail) bekommt keinen;
  // payload.title enthält Projekt-Titel, url zeigt auf /projects/<id>.
});
```

(Die exakten Fixture-Namen an die realen Testdateien anpassen — Payload-Assertions wie oben. RED zuerst: die neuen Parameter existieren noch nicht → Compile-/Laufzeit-Fehler zählt als RED.)

- [ ] **Step 2: Implementierung**

`jobs.ts` — Signatur von `sendDueReminders` um letzten optionalen Parameter `pushSender?: PushSender | null` erweitern (Import `type PushSender`, `pushToUsers` aus `./push.js`). Direkt neben dem bestehenden `mailer.sendReminder`-Aufruf (NACH dem Mail-Dispatch, unabhängig davon):

```ts
      // Zweitkanal Web-Push (Backlog 7): nur User-Slots (Gäste haben keine Subscription).
      if (pushSender && slot.userId) {
        pushToUsers(prisma, pushSender, [slot.userId], {
          title: `24pray — deine Gebetsstunde beginnt bald`,
          body: projectTitle, // Variablennamen an den lokalen Scope der Schleife anpassen
          url: `${env.APP_URL}/projects/${slot.projectId}`,
        }).catch((err) => console.error('[push] reminder push failed:', err));
      }
```

(Falls `env` im Job nicht verfügbar ist: wie der Mail-Pfad es löst — an die real vorhandene Struktur anpassen, URL-Form beibehalten.)

`server.ts` — die `sendDueReminders(...)`-Aufrufstelle (grep) um das neue Argument erweitern; `createPushSender(env)` dort einmal erzeugen und wiederverwenden.

`community.ts` — Deps um `pushSender?: PushSender | null`; im Fan-out-Block NACH der Mail-Schleife:

```ts
        // Zweitkanal Web-Push (Backlog 7): Empfänger-E-Mails → User mit Konto → deren Geräte.
        if (pushSender && recipients.length > 0) {
          const users = await prisma.user.findMany({
            where: { email: { in: recipients.map((r) => r.email) } },
            select: { id: true },
          });
          await pushToUsers(prisma, pushSender, users.map((u) => u.id), {
            title: `24pray — Neues aus der Gebetswache`,
            body: project.title,
            url: projectUrl,
          }).catch(() => {});
        }
```

`app.ts` — `communityRoutes(app, { prisma, mailer, env, pushSender });`

- [ ] **Step 3: Tests laufen lassen — grün (komplette Suite)**

Run: `cd /home/pmi/24pray-api && npx vitest run && npx tsc --noEmit`
Expected: PASS (auch alle Alt-Tests: pushSender ist überall optional).

- [ ] **Step 4: Commit**

```bash
cd /home/pmi/24pray-api
git add src/lib/jobs.ts src/lib/jobs.test.ts src/server.ts src/routes/community.ts src/routes/community.test.ts src/app.ts
git commit -m "feat(push): Push-Zweitkanal fuer Stunden-Erinnerung + Owner-Updates (Backlog 7)"
```

---

### Task 3: Web — Service Worker, Client-Lib, Profil-Opt-in

**Files:**
- Create: `/home/pmi/24pray-web/public/sw.js`
- Create: `/home/pmi/24pray-web/src/lib/push.ts`
- Test: `/home/pmi/24pray-web/src/lib/push.test.ts`
- Create: `/home/pmi/24pray-web/src/components/patterns/PushSettingsCard.tsx`
- Test: `/home/pmi/24pray-web/src/components/patterns/PushSettingsCard.test.tsx`
- Modify: `/home/pmi/24pray-web/src/app/profil/page.tsx` (Karte einbauen, zwischen Namens-Card und DangerZone)
- Modify: `/home/pmi/24pray-web/src/lib/i18n.ts` (Keys in alle 5 Kataloge)

**Interfaces:**
- Consumes: `GET /push/vapid-key`, `POST/DELETE /me/push-subscriptions` (Task 1); `api` aus `@/lib/api`.
- Produces: `isPushSupported(): boolean`, `getPushState(): Promise<'unsupported'|'unavailable'|'denied'|'subscribed'|'unsubscribed'>`, `enablePush(): Promise<void>`, `disablePush(): Promise<void>`.

- [ ] **Step 1: `public/sw.js`**

```js
/* 24pray Service Worker (Backlog 7): nur Web-Push — kein Caching/Offline (bewusst minimal). */
self.addEventListener('push', (event) => {
  let data = { title: '24pray', body: '', url: '/' };
  try { data = { ...data, ...event.data.json() }; } catch { /* leerer/kaputter Payload */ }
  event.waitUntil(
    self.registration.showNotification(data.title, {
      body: data.body,
      icon: '/icons/icon-192.png',
      badge: '/icons/icon-192.png',
      data: { url: data.url },
    }),
  );
});

self.addEventListener('notificationclick', (event) => {
  event.notification.close();
  const url = event.notification.data?.url || '/';
  event.waitUntil(
    clients.matchAll({ type: 'window', includeUncontrolled: true }).then((list) => {
      for (const c of list) {
        if ('focus' in c) { c.navigate(url); return c.focus(); }
      }
      return clients.openWindow(url);
    }),
  );
});
```

- [ ] **Step 2: Failing Lib-Test schreiben**

`/home/pmi/24pray-web/src/lib/push.test.ts` (jsdom hat keine Push-APIs — vollständig mocken):

```ts
// @vitest-environment jsdom
import { describe, it, expect, vi, beforeEach } from 'vitest';

const apiGet = vi.fn(async () => ({ key: 'BPubKey' }));
const apiPost = vi.fn(async () => undefined);
const apiDelete = vi.fn(async () => undefined);
vi.mock('@/lib/api', () => ({ api: {
  get: (...a: unknown[]) => apiGet(...a),
  post: (...a: unknown[]) => apiPost(...a),
  delete: (...a: unknown[]) => apiDelete(...a),
} }));

import { enablePush, disablePush, isPushSupported } from './push';

function mockPushEnv(existingSub: unknown = null) {
  const subscribe = vi.fn(async () => ({
    endpoint: 'https://push.example/e1',
    toJSON: () => ({ endpoint: 'https://push.example/e1', keys: { p256dh: 'p', auth: 'a' } }),
    unsubscribe: vi.fn(async () => true),
  }));
  const registration = {
    pushManager: { subscribe, getSubscription: vi.fn(async () => existingSub) },
  };
  Object.defineProperty(navigator, 'serviceWorker', {
    value: { register: vi.fn(async () => registration), ready: Promise.resolve(registration) },
    configurable: true,
  });
  Object.defineProperty(window, 'PushManager', { value: function () {}, configurable: true });
  Object.defineProperty(window, 'Notification', {
    value: { requestPermission: vi.fn(async () => 'granted'), permission: 'default' },
    configurable: true,
  });
  return { subscribe, registration };
}

describe('push client lib (Backlog 7)', () => {
  beforeEach(() => { apiGet.mockClear(); apiPost.mockClear(); apiDelete.mockClear(); });

  it('enablePush: registriert SW, abonniert mit VAPID-Key, meldet Subscription an die API', async () => {
    mockPushEnv();
    await enablePush();
    expect(apiGet).toHaveBeenCalledWith('/push/vapid-key');
    expect(apiPost).toHaveBeenCalledWith('/me/push-subscriptions', expect.objectContaining({
      endpoint: 'https://push.example/e1',
      keys: { p256dh: 'p', auth: 'a' },
    }));
  });

  it('disablePush: kündigt Browser-Subscription und meldet sie bei der API ab', async () => {
    const sub = {
      endpoint: 'https://push.example/e1',
      toJSON: () => ({ endpoint: 'https://push.example/e1', keys: { p256dh: 'p', auth: 'a' } }),
      unsubscribe: vi.fn(async () => true),
    };
    mockPushEnv(sub);
    await disablePush();
    expect(sub.unsubscribe).toHaveBeenCalled();
    expect(apiDelete).toHaveBeenCalledWith('/me/push-subscriptions', { endpoint: 'https://push.example/e1' });
  });
});
```

WICHTIG: `api.delete` im generischen Client (`src/lib/api.ts`) unterstützt evtl. keinen Body — prüfen; falls nicht, `delete<T>(path, body?)` dort minimal erweitern (Muster `post`).

- [ ] **Step 3: Lib implementieren**

`/home/pmi/24pray-web/src/lib/push.ts`:

```ts
'use client';

import { api } from '@/lib/api';

// Web-Push Opt-in (Backlog 7): self-hosted VAPID, kein Drittdienst. Nur eingeloggte Nutzer —
// die Subscription hängt am Konto (Erinnerungen + Owner-Updates als Zweitkanal neben Mail).

export function isPushSupported(): boolean {
  return typeof window !== 'undefined'
    && 'serviceWorker' in navigator
    && 'PushManager' in window
    && 'Notification' in window;
}

function urlBase64ToUint8Array(base64: string): Uint8Array {
  const padding = '='.repeat((4 - (base64.length % 4)) % 4);
  const b64 = (base64 + padding).replace(/-/g, '+').replace(/_/g, '/');
  const raw = window.atob(b64);
  return Uint8Array.from([...raw].map((c) => c.charCodeAt(0)));
}

export type PushState = 'unsupported' | 'unavailable' | 'denied' | 'subscribed' | 'unsubscribed';

export async function getPushState(): Promise<PushState> {
  if (!isPushSupported()) return 'unsupported';
  if (Notification.permission === 'denied') return 'denied';
  try {
    await api.get<{ key: string }>('/push/vapid-key');
  } catch {
    return 'unavailable'; // Server hat Push (noch) nicht konfiguriert
  }
  const reg = await navigator.serviceWorker.register('/sw.js');
  const sub = await reg.pushManager.getSubscription();
  return sub ? 'subscribed' : 'unsubscribed';
}

export async function enablePush(): Promise<void> {
  const { key } = await api.get<{ key: string }>('/push/vapid-key');
  const reg = await navigator.serviceWorker.register('/sw.js');
  const permission = await Notification.requestPermission();
  if (permission !== 'granted') throw new Error('denied');
  const sub = await reg.pushManager.subscribe({
    userVisibleOnly: true,
    applicationServerKey: urlBase64ToUint8Array(key),
  });
  const json = sub.toJSON() as { endpoint: string; keys: { p256dh: string; auth: string } };
  await api.post('/me/push-subscriptions', { endpoint: json.endpoint, keys: json.keys });
}

export async function disablePush(): Promise<void> {
  const reg = await navigator.serviceWorker.register('/sw.js');
  const sub = await reg.pushManager.getSubscription();
  if (!sub) return;
  const json = sub.toJSON() as { endpoint: string };
  await sub.unsubscribe();
  await api.delete('/me/push-subscriptions', { endpoint: json.endpoint }).catch(() => {});
}
```

(Beim Test-Mock oben registriert `enablePush` den SW VOR der Permission-Abfrage — Reihenfolge der Implementierung und des Tests konsistent halten.)

- [ ] **Step 4: i18n-Keys (alle 5 Kataloge)**

```ts
  // de:
  pushCardTitle: 'Benachrichtigungen auf diesem Gerät',
  pushCardHint: 'Erhalte deine Stunden-Erinnerung und Updates aus deinen Wachen zusätzlich als Push — ohne Drittdienst, direkt von 24pray.',
  pushEnable: 'Push aktivieren',
  pushDisable: 'Push deaktivieren',
  pushStateSubscribed: 'Aktiv auf diesem Gerät.',
  pushStateDenied: 'Im Browser blockiert — bitte in den Website-Einstellungen erlauben.',
  pushStateUnsupported: 'Dieser Browser unterstützt keine Push-Benachrichtigungen.',
  pushStateUnavailable: 'Push ist auf dem Server noch nicht eingerichtet.',
  // en:
  pushCardTitle: 'Notifications on this device',
  pushCardHint: 'Get your hour reminder and updates from your watches as push too — no third-party service, straight from 24pray.',
  pushEnable: 'Enable push',
  pushDisable: 'Disable push',
  pushStateSubscribed: 'Active on this device.',
  pushStateDenied: 'Blocked in the browser — please allow it in the site settings.',
  pushStateUnsupported: 'This browser does not support push notifications.',
  pushStateUnavailable: 'Push is not set up on the server yet.',
  // es:
  pushCardTitle: 'Notificaciones en este dispositivo',
  pushCardHint: 'Recibe tu recordatorio de hora y las novedades de tus vigilias también como push — sin terceros, directo de 24pray.',
  pushEnable: 'Activar push',
  pushDisable: 'Desactivar push',
  pushStateSubscribed: 'Activo en este dispositivo.',
  pushStateDenied: 'Bloqueado en el navegador — permítelo en la configuración del sitio.',
  pushStateUnsupported: 'Este navegador no admite notificaciones push.',
  pushStateUnavailable: 'El push aún no está configurado en el servidor.',
  // he:
  pushCardTitle: 'התראות במכשיר הזה',
  pushCardHint: 'קבלו את תזכורת השעה ועדכונים מהמשמרות שלכם גם כהתראת דחיפה — בלי צד שלישי, ישירות מ-24pray.',
  pushEnable: 'הפעלת התראות',
  pushDisable: 'כיבוי התראות',
  pushStateSubscribed: 'פעיל במכשיר הזה.',
  pushStateDenied: 'חסום בדפדפן — אנא אפשרו זאת בהגדרות האתר.',
  pushStateUnsupported: 'דפדפן זה אינו תומך בהתראות דחיפה.',
  pushStateUnavailable: 'התראות דחיפה עדיין לא הוגדרו בשרת.',
  // ar:
  pushCardTitle: 'الإشعارات على هذا الجهاز',
  pushCardHint: 'استلم تذكير ساعتك وتحديثات سهراتك أيضًا كإشعار فوري — دون طرف ثالث، مباشرة من 24pray.',
  pushEnable: 'تفعيل الإشعارات',
  pushDisable: 'إيقاف الإشعارات',
  pushStateSubscribed: 'مفعّل على هذا الجهاز.',
  pushStateDenied: 'محظور في المتصفح — يرجى السماح به في إعدادات الموقع.',
  pushStateUnsupported: 'هذا المتصفح لا يدعم الإشعارات الفورية.',
  pushStateUnavailable: 'الإشعارات لم تُهيّأ على الخادم بعد.',
```

- [ ] **Step 5: PushSettingsCard + Einbau**

`/home/pmi/24pray-web/src/components/patterns/PushSettingsCard.tsx`:

```tsx
'use client';

import { useEffect, useState } from 'react';
import { Bell, BellOff } from 'lucide-react';
import { Card } from '@/components/ui/Card';
import { Button } from '@/components/ui/Button';
import { toast } from '@/components/ui/toast-store';
import { getPushState, enablePush, disablePush, type PushState } from '@/lib/push';
import { t } from '@/lib/i18n';

/** Push-Opt-in pro Gerät (Backlog 7) — nur für eingeloggte Nutzer sichtbar (Profil-Seite). */
export function PushSettingsCard() {
  const [state, setState] = useState<PushState | 'loading'>('loading');
  const [busy, setBusy] = useState(false);

  useEffect(() => {
    getPushState().then(setState).catch(() => setState('unsupported'));
  }, []);

  async function toggle() {
    setBusy(true);
    try {
      if (state === 'subscribed') {
        await disablePush();
        setState('unsubscribed');
      } else {
        await enablePush();
        setState('subscribed');
      }
    } catch {
      const fresh = await getPushState().catch(() => 'unsupported' as const);
      setState(fresh);
      if (fresh === 'denied') toast({ message: t('pushStateDenied') });
    } finally {
      setBusy(false);
    }
  }

  const hint =
    state === 'subscribed' ? t('pushStateSubscribed')
    : state === 'denied' ? t('pushStateDenied')
    : state === 'unsupported' ? t('pushStateUnsupported')
    : state === 'unavailable' ? t('pushStateUnavailable')
    : t('pushCardHint');

  return (
    <Card elevation={1}>
      <h2 className="text-sm font-semibold text-ink">{t('pushCardTitle')}</h2>
      <p className="mt-1 text-xs text-ink-muted">{hint}</p>
      {(state === 'subscribed' || state === 'unsubscribed') && (
        <Button
          variant="secondary"
          className="mt-4"
          icon={state === 'subscribed' ? BellOff : Bell}
          loading={busy}
          onClick={toggle}
        >
          {state === 'subscribed' ? t('pushDisable') : t('pushEnable')}
        </Button>
      )}
    </Card>
  );
}
```

Test `PushSettingsCard.test.tsx` (Lib mocken):

```tsx
// @vitest-environment jsdom
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, cleanup, fireEvent, waitFor } from '@testing-library/react';

const getPushState = vi.fn(async () => 'unsubscribed');
const enablePush = vi.fn(async () => undefined);
const disablePush = vi.fn(async () => undefined);
vi.mock('@/lib/push', () => ({
  getPushState: (...a: unknown[]) => getPushState(...a),
  enablePush: (...a: unknown[]) => enablePush(...a),
  disablePush: (...a: unknown[]) => disablePush(...a),
}));

import { PushSettingsCard } from './PushSettingsCard';

describe('PushSettingsCard (Backlog 7)', () => {
  beforeEach(() => { cleanup(); getPushState.mockClear(); enablePush.mockClear(); });

  it('unsubscribed → Aktivieren-Button, Klick ruft enablePush', async () => {
    render(<PushSettingsCard />);
    const btn = await screen.findByRole('button', { name: /aktivieren|enable/i });
    fireEvent.click(btn);
    await waitFor(() => expect(enablePush).toHaveBeenCalledOnce());
  });

  it('unsupported → kein Button, erklärender Text', async () => {
    getPushState.mockResolvedValueOnce('unsupported');
    render(<PushSettingsCard />);
    await waitFor(() => expect(screen.getByText(/unterstützt keine push/i)).toBeTruthy());
    expect(screen.queryByRole('button')).toBeNull();
  });
});
```

`profil/page.tsx` — `<PushSettingsCard />` zwischen der Namens-Card und der `DangerZone` einfügen (Import ergänzen). Falls `profil.test.tsx` bricht: `@/lib/push` dort mocken (Muster oben), Assertions unverändert.

- [ ] **Step 6: Suiten laufen lassen — grün**

Run: `cd /home/pmi/24pray-web && npx vitest run && npx tsc --noEmit && npx next lint`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
cd /home/pmi/24pray-web
git add public/sw.js src/lib/push.ts src/lib/push.test.ts src/components/patterns/PushSettingsCard.tsx src/components/patterns/PushSettingsCard.test.tsx src/app/profil/page.tsx src/app/profil/profil.test.tsx src/lib/i18n.ts src/lib/api.ts
git commit -m "feat(push): Service Worker, Client-Lib und Profil-Opt-in (Backlog 7)"
```

(api.ts nur committen, falls `delete` um Body erweitert wurde.)

---

### Task 4: Abschluss — Suiten + Backlog (KEIN Push)

**Files:**
- Modify: `/home/pmi/24pray-web/docs/BACKLOG.md`

- [ ] **Step 1: Beide Suiten komplett**

```bash
cd /home/pmi/24pray-api && npx vitest run && npx tsc --noEmit
cd /home/pmi/24pray-web && npx vitest run && npx tsc --noEmit && npx next lint
```

- [ ] **Step 2: Backlog — Punkt 7 ersetzen**

```markdown
7. ~~**Web-Push (PWA)**~~ — GEBAUT 2026-07-09 (Opt-in pro Gerät auf der Profil-Seite, nur für
   Konten; Stunden-Erinnerung + Owner-Updates als Zweitkanal neben Mail; self-hosted VAPID,
   kein Drittdienst; abgelaufene Subscriptions werden beim Senden automatisch aufgeräumt.
   Deploy: `npx web-push generate-vapid-keys` → VAPID_PUBLIC_KEY/VAPID_PRIVATE_KEY/VAPID_SUBJECT
   in /etc/24pray-api.env; ohne Keys ist das Feature serverseitig aus und die Profil-Karte
   erklärt das ehrlich.)
```

- [ ] **Step 3: Commit (nur web, kein Push)**

```bash
cd /home/pmi/24pray-web
git add docs/BACKLOG.md
git commit -m "docs(backlog): Punkt 7 Web-Push gebaut"
```
